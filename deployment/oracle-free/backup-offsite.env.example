# Bu dosyayı /etc/systemcel/backup-offsite.env yoluna 0600 izinle kurun.
# Remote, `rclone config` içinde type=crypt olan bir hedef olmalıdır.
SYSTEMCEL_OFFSITE_REMOTE=systemcel-crypt:systemcel-production
RCLONE_CONFIG=/etc/systemcel/rclone.conf
OFFSITE_BACKUP_ATTEMPTS=4
